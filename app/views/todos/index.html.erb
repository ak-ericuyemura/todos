<%= link_to 'Add a new todo', new_todo_path %>

<h3>Todos List</h3>
<ul class='todos'>
  <% @todos.each do |todo| %> 
    <%= content_tag :li, todo.title, class: 'todos' %>
  <% end %>
</ul>
  
    



